#!/bin/zsh

local cwd="$PWD"
local p="$1"
[ "$1" != "" ] && shift
if [ -d "$p" ]; then
	p="$(realpath $p)"
	cwd="$p"
fi

kitty -d $cwd -- firejail --whitelist=$p --read-write=$p nvim $p $@ &>/dev/null &
